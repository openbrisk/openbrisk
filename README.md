# openbrisk
The OpenBrisk meta repository.

1. Install meta: ```npm install -g meta```  
2. Clone this repository and execute ```meta git update``` from within the repository directory.

Now you have cloned everything.
